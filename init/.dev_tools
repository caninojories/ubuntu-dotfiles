#!/usr/bin/env bash

DOT="$HOME/Projects/github/digitalOcean-Ubuntu-Dev"
sudo apt-get update
sudo apt-get install nginx -y
sudo apt-get autoremove
sudo apt-get autoclean
sudo apt-get clean

sudo cp DOT/source/sample.conf /etc/nginx/conf.d/nginx.conf
